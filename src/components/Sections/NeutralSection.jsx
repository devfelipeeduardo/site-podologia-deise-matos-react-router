import styles from "../Sections/sections.module.css"

function NeutralSection() {
  return (
    <section className={`${styles['']} ${styles['brand-green-light-02']}`}>
        .
    </section>
  )
}

export default NeutralSection